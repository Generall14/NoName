#include <stdio.h>
#include <stdint.h>
#include "tst.h"

void slog_log(uint32_t log_id, ...)
{
}

int main()
{
	int AA = 6;
	//LOG_ERROR(AA);
	printf("... %s \n", LOG_ERROR(hfg));
	printf("... %i \n", LOG_ERROR(hfg));
	printf("... %s \n", AT);
	printf("End\n");
	return 0;
}
